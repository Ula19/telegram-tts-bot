# Основные зависимости
aiogram==3.26.0
SQLAlchemy==2.0.38
asyncpg==0.30.0
alembic==1.14.1
pydantic-settings==2.8.1

# TTS движок — Microsoft Edge TTS (бесплатный, без API-ключей)
edge-tts==7.2.8

# Утилиты
python-dotenv==1.0.1

# Ускорение asyncio (не работает на Windows!)
uvloop==0.22.1
